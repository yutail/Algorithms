/*
    The binary heap data structure is an array object that we can view as a nearly complete binary tree.
    Consider a heap A[0...n-1]. Parent(i) = (i-1)/2, Left(i) = 2i+1, Right(i) = 2i+2. A[parent(i)] >= A[i].
*/


/* A heap is an almost complete binary tree such that the value at the root is greater than or equal to the values at the left and
   right children and the left and right subtrees are also heaps.
*/

class Heap {

    
};